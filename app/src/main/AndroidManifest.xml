<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adit.penjualannya_adit">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Penjualannya_adit"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".DataProduk"
            android:exported="false" />
        <!-- 1. Halaman Login (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 2. Halaman Daftar -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- 3. Dashboard Utama -->
        <activity
            android:name=".MainActivity2"
            android:exported="false" /> <!-- 4. Halaman Profil Akun -->
        <activity
            android:name=".AkunActivity"
            android:exported="false" /> <!-- Fitur Kategori -->
        <activity
            android:name=".kategori.DataKategoriActivity"
            android:exported="false" />
        <activity
            android:name=".kategori.ModKategoriActivity"
            android:exported="false" />
        <activity
            android:name=".kategori.DetailKategoriFragment"
            android:exported="false" /> <!-- Fitur Produk -->
        <activity
            android:name=".TambahProduk"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Fitur Pegawai -->
        <activity
            android:name=".PegawaiActivity"
            android:exported="false" /> <!-- Fitur Cabang -->
        <activity
            android:name=".CabangActivity"
            android:exported="false" /> <!-- Fitur Printer/Nota (dari TokoKita) -->
        <activity
            android:name=".PrinterActivity"
            android:exported="false" /> <!-- === FITUR BARU DARI TOKOKITA === -->
        <!-- Transaksi / Kasir -->
        <activity
            android:name=".DataTransaksiActivity"
            android:exported="false" /> <!-- Laporan Penjualan -->
        <activity
            android:name=".LaporanActivity"
            android:exported="false" /> <!-- Layanan -->
        <activity
            android:name=".LayananActivity"
            android:exported="false" /> <!-- Data Pelanggan -->
        <activity
            android:name=".DataPelangganActivity"
            android:exported="false" />
    </application>

</manifest>